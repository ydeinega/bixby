structure (AlternativeTranslation) {
  description (alternative translation used for quizzes)
  
  property (text) {
    type (viv.core.Text)
    min (Required)
    max (One)
    visibility (Private)
  }
  
  property (translation) {
    type(Translation)
    min(Required)
    max(One)
  }
}
