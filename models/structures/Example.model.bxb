structure (Example) {
  description (example of the use of the word with sound)
  
  property (text) {
    type (viv.core.Text)
    min (Optional)
    visibility (Private)
  }
  
   property (soundUrl) {
    type (viv.core.Text)
    min (Optional)
    visibility (Private)
  }
}
