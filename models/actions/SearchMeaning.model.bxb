action (SearchMeaning) {
  description (find the most common meaning of the word)
  collect {
    input (word) {
      type (Word)
      min (Required)
      max (One)
    }
  }
  type(Search)
  output (Meanings)
}
