capsule {
  id (playground.dictionary)
  version (0.1.0)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }
  capsule-imports {
    import (viv.core) { 
      as (core) 
    }
    import (viv.image) {
      as (image) version (2.12.0)
    }
  }
}
